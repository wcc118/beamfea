"""FastAPI server. Port 1337. Endpoints per foundation_spec.md §12.

To be implemented in Stage 6.

The main() function is wired to the `beamfea` console script in pyproject.toml,
so once implemented, `beamfea` from the shell launches the server.
"""

PORT = 1337


def main() -> None:
    """Entry point for the `beamfea` console script.

    Stage 6: replace this stub with uvicorn.run(app, host='127.0.0.1', port=PORT).
    """
    raise NotImplementedError(
        "Stage 6 not yet implemented. See foundation_spec.md §12."
    )
